name: parsepesa
description: Official Flutter SDK for ParsePesa - The M-Pesa SMS Parsing API. Convert raw M-Pesa SMS notifications into structured JSON data instantly.
version: 2.1.2
homepage: https://parsepesa.nexoracreatives.co.ke
repository: https://github.com/Iconia7/parsepesa-sdks
issue_tracker: https://github.com/Iconia7/parsepesa-sdks/issues

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  http: ^1.6.0
  intl: ^0.20.0
  json_annotation: ^4.11.0
  crypto: ^3.0.3


dev_dependencies:
  lints: ^2.1.0
  test: ^1.24.0
